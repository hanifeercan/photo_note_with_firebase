name: photo_note_with_firebase
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^4.9.2
  cupertino_icons: ^1.0.8
  firebase_auth: ^4.10.0
  firebase_core: ^2.16.0
  flutter_riverpod: ^2.6.1
  freezed_annotation: ^3.1.0
  go_router: ^11.0.0
  riverpod_annotation: ^2.6.1
  url_strategy: ^0.2.0
  validators: ^3.0.0
  image_picker: ^1.0.4

dev_dependencies:
  build_runner: ^2.5.4
  custom_lint: ^0.7.6
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  freezed: ^3.1.0
  riverpod_generator: ^2.6.5
  riverpod_lint: ^2.6.5

flutter:
  uses-material-design: true